import { StyleSheet } from 'react-native';

export const SignupFormStyles = (styles: any) =>
	StyleSheet.create({
		Container: {
			...styles.FormContainer,
		},
		Button: {
			marginTop: 24,
		},
	});
