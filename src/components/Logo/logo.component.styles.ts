import { StyleSheet } from 'react-native';

export const LogoStyles = StyleSheet.create({
	Image: {
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
		resizeMode: 'contain',
		margin: 12,
	},
});
